{
  "name": "pomodoro_app",
  "version": "1.0.0",
  "description": "A responsive pomodoro app",
  "main": "index.js",
  "repository": "git@github.com:once-upon-a-tam/pomodoro-app.git",
  "author": "Tam <tam.pro@outlook.fr>",
  "homepage": "once-upon-a-tam.github.io/pomodoro_app/",
  "scripts": {
    "start": "parcel ./index.html",
    "build": "parcel build ./index.html --public-url ./ --dist-dir ./build",
    "predeploy": "rm -rf dist && parcel build index.html --no-source-maps --public-url ./",
    "deploy": "gh-pages -d build"
  },
  "assetsPath": "assets",
  "dependencies": {
    "sass": "^1.54.3"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "parcel": "^2.7.0"
  }
}
